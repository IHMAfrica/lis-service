apiVersion: v1
kind: Service
metadata:
  name: {{ include "lis-service-chart.fullname" . }}
  labels:
    {{- include "lis-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lis-service-chart.selectorLabels" . | nindent 4 }}
