apiVersion: v1
kind: Service
metadata:
  name: {{ include "lis-chart.fullname" . }}
  labels:
    {{- include "lis-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lis-chart.selectorLabels" . | nindent 4 }}
